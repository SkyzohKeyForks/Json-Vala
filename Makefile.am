ACLOCAL_AMFLAGS = -I m4
    SUBDIRS = src
    EXTRA_DIST = autogen.sh

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = json-vala.pc

vapidir = $(datadir)/vala/vapi
dist_vapi_DATA = json-vala.deps
